FROM python:3.10 AS builder

WORKDIR /code

COPY requirements.txt .
RUN pip install --no-cache-dir --upgrade -r requirements.txt

COPY ./app /code/app

# EXPOSE 3000

CMD ["uvicorn", "./app.py", "--proxy-headers", "--host", "0.0.0.0", "--port", "80"]